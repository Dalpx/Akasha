import 'package:flutter/material.dart';

class TransaccionView extends StatelessWidget {
  const TransaccionView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Transacción"),),
    );
  }
}